function sayHello(name) {
    debugger;
    return `Hello ${name}`;
}

const firtName = "Redana";

console.info(sayHello(firtName));


//cara menjalankannya file debugger dengan perintah node inspect 'name file'